pipeline {
    agent any
    stages {
        stage('check version of git, maven,java') {
            steps {

			        sh 'git --version'
                    sh 'java --version'				
			     }
        }
        
    }
}
